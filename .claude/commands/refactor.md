Start a safe refactoring session using the refactorer agent.
Identify the refactor type (extract, inline, rename, restructure, generalize).
Read all callers first. Run `uv run pytest` after each step.
Never break public API without approval.
